# Next.js Frontend — Vercel Environment Variables
# Set these per-project in Vercel dashboard

# Django backend URL (Railway)
NEXT_PUBLIC_API_BASE_URL=https://your-backend.railway.app

# Tenant slug — determines which tenant config to load
# salon-x | restaurant-x | health-club-x
NEXT_PUBLIC_TENANT_SLUG=salon-x
